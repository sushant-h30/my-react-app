pipeline:
  name: apex-workflow
  identifier: apexworkflow
  description: CI pipeline for React app - builds, lints, and tests
  projectIdentifier: apexworkflow
  orgIdentifier: default
  tags:
    ai_generated: ""
  stages:
    - stage:
        name: Dev
        identifier: Dev
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          caching:
            enabled: true
            override: true
          buildIntelligence:
            enabled: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Docker
            spec:
              harnessImageConnectorRef: account.harnessImage
          execution:
            steps:
              - step:
                  type: Run
                  name: Build and Test
                  identifier: Run_1
                  spec:
                    shell: Sh
                    command: |-
                      set -e

                      $HARNESS_WORKSPACE/bi/auto-injection || true

                      export NVM_DIR="/home/sushant/.nvm"
                      . "$NVM_DIR/nvm.sh"
                      nvm use 24

                      echo "=== Node Environment ==="
                      which node
                      which npm
                      node -v
                      npm -v
                      node -p "process.platform"
                      npm config get cache

                      echo "=== Workspace Permissions ==="
                      whoami
                      id
                      ls -ld "$HARNESS_WORKSPACE"
                      ls -l "$HARNESS_WORKSPACE/package-lock.json"

                      echo "=== Building React App ==="

                      npm install --package-lock=false
                      npm run lint
                      npm run build

                      echo ""
                      echo "=== Build Complete ==="
              - step:
                  type: GCSUpload
                  name: GCSUpload_1
                  identifier: GCSUpload_1
                  spec:
                    connectorRef: GCP_Connector
                    bucket: gs://my-react-poc
                    sourcePath: dist
  properties:
    ci:
      codebase:
        connectorRef: Git_Account
        repoName: my-react-app
        build: <+input>
